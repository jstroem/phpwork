# Simple php boilerplate for webprojects

Using html 5 boilerplate as default and is configured to work with the buildscript to minify js, css and html.
Using mustache as template engine to seperate logic and template
Using a express.js inspired rounting system to easy configure routes.